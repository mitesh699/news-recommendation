# API Configuration
# This matches our integration approach of including /api in the base URL
REACT_APP_API_URL=http://localhost:8001/api

# For production on Vercel, this will be set to /api in vercel.json
# REACT_APP_API_URL=/api
